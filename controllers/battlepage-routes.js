const router = require('express').Router();

// All the 'GET' Routes go here... Frikin all of them.

// router.get('/', async (req, res) => {
//   res.render('battlepage');
// })

module.exports = router;