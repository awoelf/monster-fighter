<div id="battlefield" class="vh-100 vw-100 container-fluid d-flex flex-row">
    {{> menu}}
    <div class="col-10 p-0 d-flex flex-column mx-auto justify-content-between">
        <div id="opponent-area" class="d-flex justify-content-center">
            {{> health}}
            {{> extra-small-card}}
            {{> extra-small-card-back}}
            {{> extra-small-card-back}}
            {{> extra-small-card-back}}
            {{> extra-small-card-back}}
            {{> mana}}
        </div>
        <div id="playing-field">
            <div class="box m-auto field-box">
                <div class="d-flex justify-content-around opponent-field">
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                </div>
                <div class="d-flex justify-content-around player-field">
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                    {{> blank-card}}
                </div>
            </div>
        </div>
        <div id="player-area">
            <div class="d-flex flex-row align-items-end justify-content-center">
                {{!-- Mouseover a small card to expand it --}}
                <div class="col-1 m-1">
                    {{> chat-log}}
                    {{> health}}
                </div>
                {{> small-card}}
                {{> small-card}}
                {{> small-card}}
                {{> small-card}}
                {{> small-card}}
                {{> small-card-back}}
                <div class="col-1 m-1">
                    {{> mana}}
                </div>
                
            </div>
        </div>
    </div>
    
    
</div>