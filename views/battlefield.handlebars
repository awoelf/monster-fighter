<div id="battlefield" class="vh-100 vw-100 d-flex flex-column justify-content-between">
    {{> menu}} 
    <div class="row d-flex justify-content-center align-items-center">
        <div class="box">
            <h3 class="box-text m-2">User 1</h3>
        </div>
        {{!-- Temporary for debugging --}}
        <div class="d-flex flex-column align-items-center">
            {{> card}}
        </div>
        <div class="col-2"></div>
        <div class="d-flex flex-column align-items-center">
            {{> card}}
        </div>
        <div class="box">
            <h3 class="box-text m-2">User 2</h3>
        </div>
    </div>
    <div class="d-flex align-items-end">
        <div class="col-sm-2 col-lg-4 p-0">
            <div>
                <button type="button" class="btn small-btn px-2 pt-3">
                    <span class="material-symbols-outlined m-auto p-0">chat_bubble</span>
                </button>
            </div>
            <div class="log m-2 p-3 d-flex flex-column justify-content-end">
                <p class="box-text m-0">Actions log</p>
            </div>
        </div>
        <div class="col-sm-6 col-lg-8 d-flex justify-content-around p-0">
            {{> small-card}}
            {{> small-card}}
            {{> small-card}}
            {{> small-card}}
            {{> small-card}}
        </div>
    </div>
</div>